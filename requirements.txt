# Core ML stack — Blackwell-optimized wheels
torch>=2.4.0
transformers>=4.45.0
accelerate>=0.34.0
tokenizers>=0.20.0
flash-linear-attention[cuda] >= 0.5.0
kernels==0.15.2

# pip install vllm --extra-index-url https://download.pytorch.org/whl/cu132
vllm>=0.6.0

# Hardware telemetry — nvidia-ml-py ships the pynvml module; the legacy pynvml fork is deprecated
nvidia-ml-py>=13.610.43
psutil>=6.0.0

# Config & validation
pydantic>=2.8.0
PyYAML>=6.0.2

# OpenAI-API-compatible client — also used for OpenRouter, Gemini, and
# LLM-as-a-judge (api backend / api judge provider)
openai>=1.40.0

# Dataset access
datasets>=3.0.0
huggingface-hub>=0.25.0

# Storage
pyarrow>=17.0.0
pandas>=2.2.0
fastparquet>=2024.2.0

# Utilities
tqdm>=4.66.0

# Testing
pytest>=8.0.0
